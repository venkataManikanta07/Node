const {AddFn, SubFn} = require("./math")

console.log(AddFn(2,3));
console.log(SubFn(2,3));


/**
 * 1. We are importing the functions AddFn and SubFn from the math module using require.
 * 2. We are then calling these functions with the arguments 2 and 3 and logging the results to the console.
 * 3. Similalry we can import other packages like "fs", "http", "Crypto" etc and use their functions in our code.
*/